{
  "name": "gupshup",
  "version": "1.0.0",
  "description": "WhatsApp-like social media app with messaging, video/audio calls, and file sharing",
  "main": "index.js",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace backend && npm install --workspace frontend",
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "start": "npm start --workspace backend"
  },
  "keywords": ["messaging", "video-call", "audio-call", "file-sharing", "social-media"],
  "author": "syed906",
  "license": "MIT"
}
